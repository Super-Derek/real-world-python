# Real World Python — Lab Workspace

Each chapter is self-contained with its own virtual environment and `requirements.txt`.

## Usage

- `cd ch01_saving_shipwrecked_sailors_with_bayes_rule`
- `source .venv/bin/activate`
- Install extra deps with `pip install ... && pip freeze > requirements.txt`
- Code goes in `src/`.

